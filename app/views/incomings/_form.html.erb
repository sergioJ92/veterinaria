<%= form_with(model: incoming, local: true) do |form| %>
  <% if incoming.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incoming.errors.count, "error") %> prohibited this incoming from being saved:</h2>

      <ul>
      <% incoming.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Detalle" %>
    <%= form.text_field :details, id: :incoming_details %>
  </div>

  <div class="field">
    <%= form.label "Fecha" %>
    <%= form.text_field :date, id: :incoming_date %>
  </div>

  <div class="field">
    <%= form.label "Monto" %>
    <%= form.text_field :ammount, id: :incoming_ammount %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", :class => 'btn btn-primary' %>
  </div>
<% end %>
