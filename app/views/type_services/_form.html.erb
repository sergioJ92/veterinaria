<%= form_for @type_service, :html => { :class => "form-horizontal type_service" } do |f| %>

  <% if @type_service.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@type_service.errors.count, "error") %> prohibited this type_service from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @type_service.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "Nombre", :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit "Aceptar", :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("Cancelar")),
                type_services_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
